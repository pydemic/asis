defmodule AsisWeb.RoraimaLive.ErrorManager do
  # defp handle_error(socket, reason) do
  #   Logger.error(reason)

  #   socket
  #   |> put_flash(:error, get_error_message(reason))
  #   |> redirect(to: "/")
  # end

  # defp get_error_message(_reason) do
  #   "Falha ao filtrar dados."
  # end
end
